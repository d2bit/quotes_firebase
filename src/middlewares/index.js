import authentication from './authentication'
import authorization from './authorization'
import database from './database'

export {authentication, authorization, database}

export default {
  authentication,
  authorization,
  database,
}

